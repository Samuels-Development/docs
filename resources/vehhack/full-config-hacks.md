# Full Hacks Config Reference

The complete default `hacks.lua` for **Vehicle Hacking Device**. This file defines every hack the device can run — add, remove, tune, or disable individual hacks here.

::: tip
For the full per-hack field reference (including `executeTime`, `hackedFx`, `requiresSeat`, `requiresSpeed`, `speedThreshold`, `fuelLevel`, …), see the [Configuration → Hacks Table](./configuration#hacks-table) page.
:::

<a href="/configs/sd-vehhack-hacks.lua" download class="download-link">Download hacks.lua</a>

<<< @/public/configs/sd-vehhack-hacks.lua{lua}
